ssh_root_shosts:
 - head
 - '{{ hostvars["head"]["network_config"]["interfaces"]["eth1"]["ipv6_address"] }}'

ssh_shosts_equiv:
 - head
 - '{{ hostvars["head"]["network_config"]["interfaces"]["eth1"]["ipv6_address"] }}'
